openapi: 3.2.0
info:
  title: Example API for testing Schema-First
  version: 1.0.1
paths:
  /endpoint:
    get:
      operationId: endpoint
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                type: object
          description: OK
